# איפוס מצב מוגן - מדריך חירום

## הבעיה
אם שכחת את הסיסמה של המצב המוגן ואין לך דרך לבטל אותו מתוך האפליקציה.

## מה השתנה? (עדכון חדש)

**החל מעדכון זה, איפוס הגדרות מלא דורש סיסמה במצב מוגן!**

כאשר המצב המוגן מופעל, פעולת "איפוס הגדרות" בהגדרות המתקדמות תדרוש הזנת סיסמה לפני ביצוע האיפוס. זה מונע איפוס מקרי או לא מורשה של כל ההגדרות.

## הפתרון

### אופציה 1: הרצת סקריפט (מומלץ)

1. **סגור את האפליקציה Otzaria** (חשוב!)
2. **הפעל את הקובץ**: `reset_protected_mode.bat`
   - פשוט לחץ פעמיים על הקובץ
3. **פתח את האפליקציה מחדש**
4. המצב המוגן יהיה מבוטל

### אופציה 2: הרצה ידנית מטרמינל

```bash
# סגור את האפליקציה קודם!
dart run reset_protected_mode.dart
```

### אופציה 3: מחיקה ידנית של קובץ ההגדרות

אם הסקריפט לא עובד, אפשר למחוק את קובץ ההגדרות ידנית:

1. **סגור את האפליקציה**
2. **מצא את תיקיית ההגדרות**:
   - Windows: `%APPDATA%\otzaria\` או `%LOCALAPPDATA%\otzaria\`
3. **מחק את הקובץ** `app_preferences.hive` (או את כל התיקייה)
4. **פתח את האפליקציה מחדש**

⚠️ **שים לב**: מחיקת קובץ ההגדרות תאפס את כל ההגדרות של האפליקציה, לא רק את המצב המוגן!

## מה הסקריפט עושה?

הסקריפט מאפס שני ערכים בהגדרות:
- `key-protected-mode-enabled` → `false` (מבטל את המצב המוגן)
- `key-protected-mode-password-hash` → `''` (מוחק את הסיסמה)

## אחרי האיפוס

לאחר האיפוס, תוכל:
1. להיכנס להגדרות בלי סיסמה
2. להגדיר סיסמה חדשה אם תרצה
3. להפעיל מחדש את המצב המוגן

## פתרון בעיות

### הסקריפט לא עובד?
- וודא שסגרת את האפליקציה לפני הרצת הסקריפט
- נסה להריץ מטרמינל עם הרשאות מנהל
- בדוק שיש לך Flutter מותקן (`flutter --version`)

### עדיין לא עובד?
- נסה את אופציה 3 (מחיקה ידנית)
- פנה לתמיכה עם פרטי השגיאה

---

**נוצר ב**: 2026-02-11
**עודכן ב**: 2026-02-11
**גרסה**: 1.1
