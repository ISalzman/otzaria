# ריבוי חלונות — מסמך מסירה

**ענף:** `spike/p-0` · **בסיס:** `chapter0/tabs-hardening` · **29 קומיטים** ·
לא נדחף, אין PR.

> **עודכן אחרי סבב ה-QA.** חמישה חוסמי מיזוג, חמישה פערים תפקודיים ושתי
> סתירות תיעוד נסגרו בחמישה קומיטים נוספים. הסעיפים 1, 4 ו-6 שוכתבו;
> §5 (תחרות ה-thread) נשאר כשהיה, כי הוא **לא** נסגר.

מסמכים נלווים: `P-0-stage3-result.md` (ההכרעה הארכיטקטונית),
`P-2-two-windows.md` (מדידות שני חלונות), `multi-window.md` (המימוש),
`T-1.3-window-registry.md` (רישום החלונות — מסמך ההחלטות).
מפת הדרכים המקורית: `C:\Users\User\Downloads\‏‏אוצריא-גרירת חלונות כמו דפדפן מודרני.md`
(גרסה 13, נספחים יא'–יב' נוספו על ידי).

---

## 1. מה הושג

חלונות אוצריא מרובים בתהליך אחד, עם גרירת כרטיסיות ביניהם ואל שולחן
העבודה, בסגנון דפדפן.

| יכולת | מצב |
|---|---|
| "העבר לחלון חדש" בתפריט ההקשר | ✅ |
| "העבר לחלון קיים" + תת-תפריט | ✅ |
| גרירת כרטיסיה החוצה → חלון חדש | ✅ |
| גרירה לחלון אחר, למקום מדויק ברצועה | ✅ |
| תצוגת כרטיסיה נגררת מחוץ לחלון | ✅ |
| קו חיווי בחלון היעד | ✅ |
| תקרת 4 חלונות + הודעה | ✅ |
| סגירת חלון בודד | ✅ |
| `Ctrl+Shift+T` — כרטיסיה ואז חלון | ✅ מכל מסך |
| שיתוף היסטוריה/סימניות/שולחנות עבודה | ✅ עם בורר גרסאות |
| שמירת כרטיסיות בחלון משני | ✅ מפתח סשן פר-חלון |
| סנכרון הגדרות **חי** | ✅ |
| ירושת גודל מהפותח | ✅ |
| שיתוף הערות אישיות | ❌ הוסר — לא היה קיים מעולם |

> ⚠️ **"שיתוף הערות" היה הצהרה שקרית.** `sharedBoxes` מנה `notes`, ואין
> box בשם הזה. הערות אישיות יושבות ב-`app_preferences`, וכעת הן מסונכרנות
> דרך סנכרון ההגדרות ולא דרך ניתוב לבעלים.

**מדידות:** נראה למשתמש 422ms · תוכן מלא 1797ms (מ-3620ms) ·
שני חלונות + מאגרים 841MB · אפס שגיאות בהרצה.

**בדיקות:** 11,214 עוברות · 7 נכשלות, כולן ברשימת "נכשלות על עץ נקי"
(§1.6 במפת הדרכים) ואומתו כנכשלות גם בהרצה מבודדת.

---

## 2. ההכרעה הארכיטקטונית ולמה

מפת הדרכים הציגה שלוש חלופות והשאירה את ההכרעה פתוחה. הרצתי ספייק
בשלושה שלבים.

**מודל B (תהליכים נפרדים, ספרייה משותפת) — נפסל.** המופע השני נכשל
ב-`initHive`: `hive_ce` נועל את קובצי ה-`.lock` בלעדית (errno 33).

**מודל C1 (broker + תהליך UI לכל חלון) — אפשרי אך יקר.** מנוע ללא
`FlutterViewController` מריץ Dart במלואו, כולל Tantivy (90,047 תוצאות).
אבל המדידה הפכה את האינטואיציה:

```
תהליך + מנוע, בלי מאגרים        408MB
+ Hive + SQLite + Tantivy       462MB    ← הנתונים עולים 54MB בלבד
תהליך + שני מנועים              746MB
```

היקר הוא התהליך והמנוע, לא הנתונים. לכן C1 יקר מ-A בהכרח: broker של
462MB קבועים ועוד בסיס תהליך לכל חלון. **אין N שבו C1 חוסך.**

**מודל A נבחר.**

### התיקון החשוב למסמך

§3.3 טען ש-N מנועים בתהליך אחד מתחרים על thread יחיד, ושזה בלתי-פתיר
בתוך A. מדדתי:

```
thread לכל מנוע:     max-gap 102ms     (קצב מושלם)
thread משותף (בקרה): max-gap 2092ms    (קפוא לגמרי)
```

פי 20. הבקרה הכרחית — בלעדיה 102ms יכול היה להעיד שלולאת השריפה לא
חסמה. המסקנה: **התחרות אמיתית, ו-thread לכל מנוע פותר אותה בתוך A.**

⚠️ **וכאן הנפילה הגדולה של הפרויקט הזה:** בהמשך התברר שאי אפשר ליצור
מנוע על thread ייעודי בזמן שחלון אחר רץ. ראו §5.

---

## 3. מה נבנה — לפי קומיטים

הספייקים (`cc75ea3a`…`790c97df`) הם מדידות; הקוד שלהם נמחק ב-`432a6d37`
והממצאים תועדו.

| קומיט | מטרה |
|---|---|
| `fe06ec5a` | **Job Object היה שייך לחלון.** סגירת חלון משני הורתה לגרעין להרוג את התהליך. באג שהיה בקוד לפני הספייק |
| `4650acad` | הפיצ'ר הראשון — "העבר לחלון חדש" |
| `b0e06096` | **יצירת מנוע חייבת להיות על ה-thread הראשי.** קריסה עקבית אחרת |
| `74dd08fa` | החלון המשני שכפל את האתחול (`RustLib` פעמיים באותו isolate) |
| `31286158` | ירושת ההגדרות — בלעדיה החלון החדש לא ידע איפה הספרייה |
| `bafd1e11` | ניווט למסך הקריאה, תקרת 4, סגירה עצמאית |
| `7cbb7891` | **אפיק ההודעות** (`IsolateNameServer`) + 12 בדיקות |
| `20567a05` | 4 תיקונים, אחד מהם **אובדן מידע** — ראו §6 |
| `8d69576a` | "העבר לחלון קיים", ביטול 403, עיצוב ההודעה |
| `9f74b364` | שורש הנתונים הפרטי רוקן את התוספים |
| `91c6faa6` | דילוג על עבודה פר-תהליך, העלאה לחזית |
| `8637baf2` | **שיתוף חי** של היסטוריה/סימניות/שולחנות/הערות |
| `eb907d46` | **סגירת חלון הקריסה את התהליך.** חלון מוסתר ולא נהרס |
| `095d2148` | **גרירה** בין חלונות ואל שולחן העבודה |
| `494b77d2` | חשיפה בפריים הראשון — 422ms |
| `ec40e8b2` | תצוגת גרירה נייטיבית + שימוש חוזר בחלון + `Ctrl+Shift+T` |
| `432a6d37` | קו חיווי, מיזוג מדויק, ניקוי 718 שורות ספייק |

---

## 4. המנגנונים

### אפיק ההודעות — `lib/core/windowing/window_bus.dart`

isolates נפרדים אינם חולקים זיכרון. `IsolateNameServer` של המנוע הוא
רישום **גלובלי לתהליך** — המנגנון היחיד שמאפשר להם לדבר בלי נייטיב.

כל חלון תופס **משבצת** מ-`otzaria.window.1..4`. לא רישום דינמי, כי אין
ספרייה מרכזית ואין חלון שמובטח שיהיה חי.

**בנוסף, הבעלים רושם כינוי:** `otzaria.window.owner`. איתורו היה סריקת
`describe` עם timeout, והוא דווקא **עסוק** 2,092ms בזמן שנפתח חלון שני —
כלומר הסריקה פקעה בדיוק כשהיא נחוצה, וזה היה מסלול אובדן הנתונים החמור
בענף. `lookupPortByName` סינכרוני ואינו יכול לפקוע.

⚠️ שלוש נקודות שהבדיקות מכסות: **משבצת רשומה בלי מאזין** (חלון שקרס) —
`peers` שואל בפועל; **timeout חובה** — חלון עסוק היה תולה את הקורא;
ו-**קידומת משבצות פר-סוויטה** — `IsolateNameServer` גלובלי לתהליך, ושתי
סוויטות תפסו את אותן המשבצות (4 מתוך 6 נכשלו בהרצה קרה).

### שיתוף מאגרים — `shared_hive_store.dart`

`storage_backend_vm.dart:114` קורא `lockRaf.lock()` ללא תנאי ובלי דגל.
לכן שיתוף = **ניתוב לבעלים** (החלון הראשון), ולא פתיחת אותו קובץ.

נקודת החנק שאפשרה זאת: כל המאגרים עוברים דרך `HiveListRepository`
שקורא/כותב רשימת JSON יחידה.

**הבעלים הוא בורר, לא רק צינור.** לכל מפתח יש **גרסה** שהוא מקדם בכל
כתיבה; כתיבה שנשענת על גרסה שהתקדמה נדחית, והקורא מחיל את כוונתו מחדש על
הערך הטרי. אחרי כל כתיבה הוא **משדר** לשאר החלונות שהמפתח השתנה.

⚠️ **אין עותק מראה מקומי, וזה שינוי מהתכנון הראשון.** הרעיון היה שהעותק
המקומי יישאר אם הבעלים ייסגר — אבל חלון סגור מוסתר ולא נהרס, ולכן
ה-isolate של הבעלים חי וקובצי ה-Hive שלו פתוחים גם אחרי שהמשתמש סגר
אותו. העותק המקומי לא הגן על כלום, ובמקום זה נתן ל-`read` שנכשל להיראות
כמו "ריק". `authoritative: false` אומר "לא הצלחנו לשאול", וכתיבה על בסיס
כזה נחסמת.

**המיזוג מוטיפס אצל הקורא**, כי הזהות של רשומה היא getter מחושב
(`Bookmark.historyKey`, `Workspace.id`) ולבעלים יש רק JSON. שכפול הלוגיקה
מעבר לגבול ה-isolate היה מתיישן בכיוונים שונים — בדיוק כמו שקרה לשלושת
מפענחי הטאבים.

### כרטיסיות — `tabs_repository.dart`

`tabs` **כן** מנותב לבעלים, אבל כל חלון כותב תחת מפתח משלו
(`key-tabs-window-<slot>`). זה **לא** שיתוף כרטיסיות: זה מה שהופך אותן
לנשמרות בכלל. ראו `T-1.3-window-registry.md` §4 לחמשת הכללים.

### הגדרות — `settings_sync.dart`

⚠️ **לא** ניתוב לבעלים, בשונה מכל השאר. `Settings.getValue` סינכרוני
ונקרא מתוך `build`, מאות פעמים בפריים — בקשת אפיק לכל קריאה אינה באה
בחשבון. לכן: כתיבה מקומית, ואחריה **שידור**. כל חלון מחיל על ה-box שלו
וטוען מחדש את ה-`SettingsBloc`.

מפתחות `window_bounds_*` ו-`window_is_*` מוחרגים: הם מצב של חלון ולא
הגדרה של התוכנה.

### הגרירה

1. `drag_preview_window.cpp` — חלון layered שעוקב אחרי הסמן.
   ⚠️ `WS_EX_TRANSPARENT` קריטי: בלעדיו הוא מוחזר מ-`WindowFromPoint`.
2. `windowAtCursor` — `GetCursorPos` → `WindowFromPoint` →
   `GetAncestor(GA_ROOT)`. העלייה ל-root חיונית.
3. `dragOver` באפיק + `ScreenToClient` בנייטיב.
4. **מסירת הגרירה למערכת** — הסמן יוצא מחלון המקור ⇒ `ReleaseCapture`
   + `WM_NCLBUTTONDOWN`/`HTCAPTION` על **התצוגה עצמה**, ומכאן Windows
   מנהל את הגרירה. **זו הדרך היחידה ל-Snap Layouts** — אין API שפותח
   אותו. פירוט ב-`docs/multi-window.md`.
   ⚠️ **שום מנוע אינו נוצר בזמן הגרירה.** גרסה ראשונה פתחה חלון אוצריא
   מלא באמצע הגרירה ומסרה אותו; זה נדחה. מה שנמסר הוא חלון Win32 ריק
   שכבר קיים, וההחלטה לאן הכרטיסיה הולכת נופלת **בשחרור** — וזה גם מה
   שהחזיר את העברת הכרטיסיה מחלון לחלון בלי השהיות.
5. **התצוגה = הכרטיסיה והתוכן שלה** — שני צילומים מורכבים למוק של
   החלון שייפתח (`tab_drag_preview.dart`). מוקטן בגרירה בתוך החלון,
   בגודל אמיתי בגרירה החוצה.
   ⚠️ גודל הצילום **נפרד** מגודל התצוגה: צילום חלון בפיקסלים פיזיים הוא
   ~10MB בתחילת כל גרירה. `previewCaptureRatio` מגביל, ו-GDI מותח.

---

## 5. ⛔ החוב הארכיטקטוני המרכזי

**ההכרעה במודל A נשענה על thread לכל מנוע. המימוש לא הצליח לספק אותו.**

ארבע מדידות, שתיים מהן סותרות:

| | thread ייעודי | thread ראשי |
|---|---|---|
| **יצירת** מנוע | ❌ מפילה | ✅ |
| **הריסת** מנוע | ✅ | ❌ מפילה |

היצירה מכריחה את ה-thread הראשי. משם נגזרו שני דברים:

1. **כל המנועים חולקים thread** ⇒ **תחרות §3.3 חזרה.** חלון עסוק יכול
   להקפיא את השני. זה בדיוק מה שהמדידה קבעה כקריטריון עצירה בבדיקה 10
   של P-2, ולא נמדד מחדש אחרי המימוש.
2. **אי אפשר להרוס מנוע** ⇒ חלון סגור **מוסתר**. המנוע נשאר בזיכרון עד
   סגירת התוכנה.

הפכתי את (2) ליתרון — פתיחה חוזרת מיידית, אי-גידול בזיכרון,
`Ctrl+Shift+T`. אבל (1) הוא **סיכון פתוח שלא נמדד**.

**המשימה הראשונה בהמשך:** להריץ את בדיקה 10 של P-2 על המימוש בפועל. אם
ההרעה >×2 מול חלון יחיד, צריך פתרון: יצירה על ה-thread הראשי ואז
**העברת בעלות** ל-thread ייעודי (לא נבדק), או `RunOnSeparateThread`
(עובד, אבל המנוע מכריז שיוסר).

### הבדיקה מוכנה להרצה — פקודה אחת

```
set OTZARIA_MEASURE_CONTENTION=1
set OTZARIA_DEBUG_OPEN_WINDOW_MS=15000
build\windows\x64\runner\Debug\otzaria.exe
```

החלון השני נפתח אחרי 15 שניות, ממתין 5 שניות לאתחול, ואז מריץ שני שלבים
של שתי שניות: בסיס, ואז בזמן שהחלון הראשון שורף CPU. הטבלה נדפסת
ל-stdout עם p50/p95/max והיחס, ועם פסק דין מול הסף.

`ThreadContentionProbe` מודד **פער בין תקתוקי טיימר** ולא "זמן פריים",
וזו הבחנה שנדרשה: חלון Flutter בסרק **אינו מייצר פריימים** — אין
אנימציה, ולכן p95 של זמן פריים מוגדר על קבוצה ריקה. פער הטיימר הוא גם מה
שהספייק המקורי מדד בפועל, ולכן 102ms ו-2,092ms ניתנים להשוואה ישירה.

⚠️ בקשת השריפה באפיק **מגודרת במשתנה הסביבה**. בקשה שמקפיאה חלון אחר
אסור שתהיה נגישה בבנייה רגילה.

---

## 6. מה עוד לא בוצע

### חסמים אמיתיים

| פריט | פירוט |
|---|---|
| **תחרות thread** | §5. לא נמדד אחרי המימוש |
| **`exit()` בזמן שמנוע אחר חי** | קריסה חוצת-threads ב-~1%. **לא נפתר**, וראו התיקון לתיוג מיד למטה |
| **P-2 בדיקות 2, 4, 5** | WebView2 environment פר-מנוע, `file_selector`, ביטול רישום מחלקת WebView — נבדקו בקוד אך **לא בזמן ריצה עם תוכן אמיתי** |

#### ⚠️ תיקון תיוג: זה אינו "מרוץ התנעה"

הפריט הזה נרשם כאן ובסיכומים כ"מרוץ התנעה בפתיחת חלון", וזה שולח את הבא
בתור למקום הלא נכון. **המדידה של P-2 §3 קבעה משהו אחר לגמרי:**

* הקריסה הבודדת הראשונה אכן נראתה בפתיחה, אבל 29 ריצות רצופות אחריה היו
  תקינות — כלומר היא לא שוחזרה שם.
* מבחן העומס **כן** שיחזר וכימת: 2 קריסות ב-200 כששני החלונות קוראים
  `exit(0)`, ו-1 ב-200 כשרק אחד קורא. ההפרש אינו מובהק, והקריסה מופיעה גם
  כשרק חלון אחד יוצא.
* המסקנה שנכתבה שם: **`exit()` בזמן שמנוע אחר חי אינו בטוח.** `exit()` של
  Dart מריץ teardown של ה-VM, וזה מה שמפיל את
  `Isolate main is owned by os thread X, failed to schedule from os thread Y`.

**מה שנגזר, ומה שלא נכתב עד כה:** המסלול הרגיל **חסין** — הוא יוצא ב-
`TerminateProcess` דרך ה-Job Object, שאינו מריץ teardown בכלל. רק מסלול
הנפילה-לאחור מגיע ל-`exit(0)`, והוא נגיש רק כשהקמת ה-Job Object נכשלה
(סביבה sandboxed, debugger, MDM/AV).

**ולמה זה מחמיר בריבוי חלונות דווקא:** `g_live_window_count` יורד בהסתרה,
כי חלון שנסגר מוסתר ולא נהרס — אבל **המנוע נשאר חי**. כלומר "נסגר החלון
האחרון" יכול לקרות בעוד שלושה מנועים רצים, וזו בדיוק התצורה המסוכנת.

נוסף `g_live_engine_count` ב-runner, והוא נשלח ל-Dart ב-`windowCount`
תחת `engines`. שורת הלוג של הסגירה המנוונת כוללת אותו עכשיו — בלעדיו אין
דרך לדעת אם קריסת יציאה שדווחה הייתה בתצורה הזו.

**מה שאין לו פתרון זמין, וחשוב לומר זאת:** הדרישה שנגזרה ב-P-2 הייתה
"לסגור כל חלון דרך ה-thread הבעלים שלו ולתת לתהליך להסתיים מעצמו".
במימוש בפועל **אין** thread בעלים נפרד, והריסת מנוע על ה-thread הראשי
**מפילה** (זו אותה מטריצה שבה יצירה עובדת רק על הראשי). כלומר הפתרון
שהמדידה הצביעה עליו חסום בדיוק על אותו חוב של תחרות ה-thread.

### לא מומש

* **`printing` מושבת בחלונות משניים.** `printing_plugin.cpp:35` מחזיק
  ערוץ יחיד ב-namespace scope, ומשייך אליו מחדש בכל
  `RegisterWithRegistrar` — כלומר רישום המנוע השני מנתב **גם** קולבקים של
  החלון הראשון ל-isolate שנרשם אחרון. עדיף שהדפסה מחלון משני תיכשל מיד
  מאשר שתשבור את הראשון.
* **macOS ו-Linux** — הצד הנייטיב הוא Windows בלבד. פרק 12 הוכרע
  ב-`docs/P-12-macos-linux.md`: ~90% מהעבודה כבר נייד (כל `lib/`), 19
  מתודות הערוץ מופו לשלוש הפלטפורמות, ו-Wayland דורש **מנגנון אחר**
  לגרירה בין חלונות — לא חיקוי של Windows. לא נכתבה שורת קוד לפלטפורמה,
  כי אין כאן toolchain לקמפל אותה.
* **פרק 3 (`ProcessServiceHost`)** לא נבנה, ולא ייבנה במודל A: אין host.
  `SharedHiveStore` + `WindowBus` מכסים את מה שנדרש בפועל.
* **T-G1.1 (`NativeWindowManager`)** — נדרש לשלושה דברים שמפורטים
  ב-`T-1.3-window-registry.md` §6, ובראשם מנגנון חוצה-תהליכים אמיתי.
* **שחזור סשן של כמה חלונות בהפעלה קרה** — היום נפתח חלון אחד, והסשנים
  של חלונות שלא נסגרו מסודר מצטרפים אליו.

### נסגר בסבב ה-QA

| מה | איפה |
|---|---|
| מודל הכתיבה של המצב המשותף — 3 חוסמי אובדן נתונים | `shared_hive_store.dart`, `hive_list_repository.dart` |
| כרטיסיות בחלון משני לא נשמרו כלל | `tabs_repository.dart` |
| חלון סגור הוצע כיעד והכרטיסיה נמחקה | `visibleSlots` + `transferTargets` |
| `openWindow` דיווח הצלחה לפני שהחלון קיים | `flutter_window.cpp` |
| שלושה מפענחי טאבים מקבילים | `tab.dart` — מפענח אחד |
| הרצועה האנכית לא שלחה כרטיסיות | `cross_window_tab_drag.dart` |
| הגדרות אינן מסונכרנות חי | `settings_sync.dart` |
| חלון משני דרס את גבולות הראשי | `window_persistence.dart` |
| T-1.3 — מסמך העיצוב | `T-1.3-window-registry.md` |
| בדיקות למסלול הגרירה | `cross_window_tab_drag_test.dart` |
| הסוויטה תלוית-סדר | `WindowBus.namespace` פר-סוויטה |

### היגיינה

* הענף נקרא `spike/p-0` אך מכיל פיצ'ר יצור. **לשנות שם לפני PR** — הוחלט
  לעשות זאת בסוף ולא באמצע, כדי לא לסכן עבודה שבתנועה.
* לא נדחף, אין PR.
* ספריית הבדיקה `C:\Users\User\Downloads\otzaria` (11.8GB) נשארה.
* **ההיסטוריה נוקתה חלקית.** ארבע שרשראות שבהן קומיט ביטל את קודמו אוחדו
  לקומיט אחד כל אחת (13 → 9 מעל `71f25f77`), והעץ אומת כזהה בייט-לבייט
  לפני האיחוד ואחריו. גיבוי: הענף `spike/p-0-backup`.
  ⚠️ `af204a7f` ("הגשר") **נשאר** בהיסטוריה כניסיון שנדחה — איחודו עם
  התיקון הסופי דורש שינוי סדר, וזה מסתיים בהתנגשויות בקבצים שהקומיטים
  שביניהם נגעו בהם. הקומיט שמחליף אותו מסביר במפורש למה הכיוון היה שגוי.
* **26 הקומיטים הראשונים (הספייק) לא נוגעו.** הם רשומה כרונולוגית של
  מדידות, וגם התוכן שלהם חי ב-`docs/P-0-stage3-result.md` ו-
  `docs/P-2-two-windows.md` — כלומר איחודם היה מוחק תיעוד ולא רעש.

---

## 7. במבט לאחור — מה היה עדיף אחרת

**שורש הנתונים הפרטי היה רחב מדי.** הפניתי את כל `getDataRootPath` כדי
לעקוף נעילת Hive, וזה רוקן את `<dataRoot>/plugins`. תפריט הכלים בחלון
משני היה ריק וכרטיסיות תוסף נעלמו. היה צריך להפנות **רק את Hive**
מלכתחילה — ההפניה הגורפת הייתה קלה יותר לכתיבה ויקרה יותר לתיקון.

**קיבעתי לפני שהרצתי בדיקות.** שיניתי חתימה של `onDragStarted` ושברתי
שני קובצי בדיקה; גיליתי אחרי הקומיט.

**שתי השערות שגויות שהצהרתי עליהן בביטחון:**
* "`RustLib.init()` הוא פר-תהליך" — הוא פר-isolate. הבעיה הייתה קריאה
  כפולה באותו isolate.
* "קריסת הסגירה היא ריאנטרנטיות" — דחיית ההריסה לא עזרה; זו אי-בטיחות
  של ההריסה עצמה.

בשני המקרים המדידה תיקנה, אבל התיעוד השגוי נכתב לפני שנבדק.

**קוד הספייק והפיצ'ר על אותו ענף.** נאלצתי למחוק 718 שורות בסוף. עדיף
היה למזג את מסקנות הספייק ולהתחיל את הפיצ'ר על ענף נקי.

**בניתי `shared_state_sync.dart` לפני שבדקתי אם הוא יכול לעבוד.** גישת
שידור השינויים חסרת משמעות כשלכל חלון קובצי Hive משלו. 92 שורות מתות.

**מה כן עבד:** להקדים מדידה להנחה. שלוש טענות מרכזיות של המסמך התבררו
כשגויות או לא-מדויקות (בדיקה 3, בדיקה 10, ה-header של Flutter), ורק
המדידה חשפה זאת.

---

## 8. QA — מה לבדוק ידנית

**גרירה**
1. גרור כרטיסיה לשולחן העבודה → חלון חדש במיקום הסמן
2. גרור אל חלון אחר, **על הרצועה** → נכנסת במקום המדויק שהקו הראה
3. גרור אל חלון אחר, **על התוכן** → נכנסת בסוף
4. גרור ושחרר בתוך אותו חלון → **שום דבר לא קורה**
5. גרור כרטיסיה **אחרונה** בחלון החוצה → נחסם
6. גרירה מעל שורת המשימות → התצוגה נראית, ושחרור שם **מבוטל**
7. גרור כרטיסיה **מהעמודה האנכית** אל שולחן העבודה → חלון חדש.
   ⚠️ זה הכיוון שלא עבד: העמודה קלטה כרטיסיות ולא שלחה
8. גרור כרטיסיה החוצה → מה שנגרר הוא **הכרטיסיה עם התוכן שלה**,
   בגודל החלון שייפתח
8א. סדר כרטיסיה ימינה/שמאלה **בתוך** החלון → נגרר מוק **מוקטן** עם
   התוכן, וראש הכרטיסיה בו חד (וידגט, לא תמונה)
8ג. באותה גרירה — המוק יושב **מתחת** לסמן, והכרטיסיות שמימין ומשמאל
   נראות זזות. אם הוא מכסה אותן, ה-`translation` חזר למרכז
8ב. בתחילת הגרירה, לפני שהצילום מוכן, נגרר ראש הכרטיסיה לבדו — מצופה
9. באותה גרירה, קרב לקצה **העליון** של המסך → **Snap Layouts נפתח**
10. שחרר על אחד האזורים → נפתח חלון אוצריא **באותה מסגרת** בדיוק.
    ⚠️ זה **נכשל** פעם אחת בשקט: האזור המוצמד מכסה את חלון המקור, `isSelf`
    יצא true, והקוד ביטל בלי הודעה ובלי לוג. אם זה חוזר — הלוג
    `גרירה הסתיימה: snapped=...` אומר מה המערכת דיווחה
11. אותה גרירה לקצה **הצדדי** → Aero Snap לחצי מסך
12. גרירה מחלון א' לחלון ב' → הכרטיסיה עוברת, ולא נפתח חלון שלישי
13. במהלך הגרירה המערכתית שאר החלונות **קופאים** — מצופה (thread משותף)
    ⚠️ ובזמן הגרירה **אין** חלון אוצריא חדש. אם מופיע אחד — המסירה
    פותחת מנוע, וזה בדיוק מה שתוקן

**חלונות**
14. פתח 4, נסה חמישי → הודעה מעוצבת
15. סגור חלון משני → התהליך חי
16. סגור את הראשון כששניים פתוחים → השני שורד
17. סגור את האחרון → התהליך יוצא
18. `Ctrl+Shift+T` אחרי סגירת חלון → משוחזר עם כרטיסיותיו
19. `Ctrl+Shift+T` **ממסך הספרייה** → עובד. הקיצור היה חסום שם
20. סגור ופתח שוב → **מיידי** (שימוש חוזר)
21. סגור חלון משני, ואז פתח תפריט הקשר של כרטיסיה →
    "העבר לחלון קיים" **אינו** מציג את החלון הסגור
22. הזז וכווץ חלון משני, סגור הכול, פתח מחדש → החלון הראשי במקום
    ובגודל **שלו**

**מצב משותף** — כל אחד מאלה היה מסלול אובדן נתונים
23. פתח חלון שני **בזמן** שהראשון טוען את הקטלוג, ואז הוסף סימנייה בכל
    אחד → שתיהן קיימות בשניהם
24. קרא ספר בא', ספר אחר בב', ופתח את ההיסטוריה בשניהם → שתי הרשומות
    בשתי הרשימות
25. סגור את א' וכתוב בב' → **לא נעלם**, וא' עדיין משרת
26. העבר ספר לחלון שני, סגור את התוכנה, פתח מחדש → הספר קיים
27. סגור חלון שני **לפני** יציאה, ואז צא ופתח → הכרטיסיות שלו **אינן**
    חוזרות (הוא נסגר במכוון)
28. שנה ערכת נושא וגודל גופן בחלון אחד → שני החלונות משתנים, ובלי
    הבהוב בגרירת המחוון

**נאמנות העברה**
29. פתח PDF, בחר מפרשים, קרב, והעבר לחלון אחר → המפרשים והתקריב עוברים
30. העבר כרטיסיית **מפרשים** → עוברת (הייתה חסומה לגמרי)
31. העבר טאב **מפוצל** → שתי החלוניות מגיעות, או שההעברה נחסמת

**מופע יחיד** (T-1.6)
32. פתח 2-3 חלונות, ואז הרץ `otzaria.exe` שוב → עולה **החלון הראשי**,
    לא חלון משני שרירותי
33. סגור חלון משני (הוא מוסתר), ואז הרץ `otzaria://` → מגיע לחלון הראשי
    הנראה, לא לחלון המוסתר
34. `otzaria.exe info` בזמן ריצה → **אינו** מעלה חלון

**עיצוב** — אינו נוגע לריבוי חלונות, אבל נשבר בדרך
39א. אין רווח כהה בין ראש הכרטיסיה הפעילה לסרגל שמתחתיה. ⚠️ שלושה
   מקורות אפשריים, וכולם נבדקו: מירכוז הכרטיסיה בסרגל
   (`kTabBackgroundBottomOffset`), הצל העליון של `AppTopBar`
   (`_ShadowBelowOnlyClipper`), והפרש הצבע ביניהם (זהים)
39ב. הצל **מתחת** ל-`AppTopBar` נשאר — הוא מפריד אותו מהתוכן
39ג. אותו דבר בחלונית של טאב מפוצל, שם `AppTopBar` אינו מתחת לרצועה

**רגרסיה בחלון יחיד**
35. אין הודעת 403, אין התראות כפולות
36. תפריט כלים מלא בשני החלונות
37. סרגל כותרת מותאם, בלי מסגרת Windows כפולה
38. לוח שנה עובד בחלון משני (אזורי זמן)
39. הוסף סימנייה, שמור שולחן עבודה, סגור ופתח → הכול כמו קודם.
    ⚠️ כל שכבת המצב המשותף מתקצרת לגישה ישירה כשאין חלון שני

**עומס** — פתח/סגור 20 פעם ובדוק שה-RSS אינו מטפס.

---

## 9. הערות למי שממשיך

**מדידות בקוד בכוונה.** `_timedPhase` ו-`[window] נראה למשתמש` פועלים
בחלון משני בלבד. `OTZARIA_DEBUG_OPEN_WINDOW_MS=50000` פותח חלון דרך
המסלול המלא של Dart.

**הכלל שחזר שוב ושוב:** כשל בספייק הוא **קודם כול חשד לטעות בספייק**.
נתפסו כך: שם נכס שגוי, `RustLib.init()` חסר, ו-header של Flutter
שסותר את ההתנהגות. גם ההפך — טענות במסמך שהתבררו כלא-מבוססות.

**בקרה לכל מדידה.** 102ms לבדו חסר ערך; רק 2092ms בבקרה נתן לו משמעות.

**אל תסיר את הסדר בסגירה.** `_isLastWindowClosing` נשאל **פעם אחת**;
שאלה חוזרת אחרי ה-flush נותנת חצי כיבוי.

**קבצים מרכזיים:**
```
lib/core/windowing/     window_bus · shared_hive_store · multi_window_service
                        window_bus_host · settings_sync · window_role
                        cross_window_tab_drag · external_tab_drag
lib/data/repository/    hive_list_repository (mutate — נתיב הכתיבה היחיד)
lib/tabs/               tabs_repository (מפתח סשן פר-חלון)
lib/core/               window_persistence (גבולות — הראשי בלבד)
windows/runner/         flutter_window.cpp (הלב) · drag_preview_window
                        win32_window.cpp (kMsgDeferredDestroy)
                        main.cpp (kMainWindowPropName — T-1.6)
lib/main.dart           secondaryWindowMain
lib/navigation/view/    custom_title_bar · reading_tab_strip
                        vertical_reading_tab_strip
```

**בנייה:** אם `cargokit` נכשל עם errno 183 — מחק את
`build/windows/x64/plugins/otzaria_search_engine/Debug/search_engine.dll`,
לא `flutter clean` (353 שניות).
