# דיבוג ותיקון לאיתור מקורות

## הבעיה המקורית ❌

כשמחפשים **"טור חושן משפט סימן ב"**, המערכת הייתה:
1. ✅ מוצאת את הספר "טור" עם התאמה לראשי תיבות (rank: 3)
2. ❌ מסירה את כל 3 הטוקנים הראשונים (טור, חושן, משפט)
3. ❌ נשאר רק "ב" במקום "סימן ב"
4. ❌ מחפשת ב-TOC "ב" ומוצאת את כל הסימנים ב' מכל 4 הטורים

## התיקון הראשון (לא מספיק) ⚠️

ניסיתי לתקן כך שתסיר רק 1 טוקן (שם הספר "טור"), אבל זה יצר בעיה חדשה:
- נשארו: "חושן | משפט | סימן | ב"
- חיפוש ב-TOC עם "חושן משפט סימן ב" לא מצא כלום
- **הסיבה**: ב-TOC של הספר "טור" כתוב "טור סימן ב" (ללא "חושן משפט")

## התיקון השני (עדיין לא מספיק) ⚠️

שיניתי את הלוגיקה כך שכאשר יש התאמה לראשי תיבות, המערכת מסירה את **כל הטוקנים שהתאימו בביטוי החיפוש** (bookQueryTokenCount), ולא רק את שם הספר.

**הקוד:**
```dart
final tokensToStrip = matchedByAcronym ? bookQueryTokenCount : 0;
```

**ההסבר:**
- `bookQueryTokenCount` = מספר הטוקנים שהתאימו בביטוי החיפוש (3 במקרה של "טור חושן משפט")
- כשיש התאמה לראשי תיבות, מסירים את כל 3 הטוקנים האלה
- נשאר רק "סימן ב" לחיפוש ב-TOC

**הבעיה שנותרה:** למרות התיקון, עדיין המערכת מצאה את סימן ב' מכל 4 הטורים, ולא רק מחושן משפט.

**הסיבה**: הספר בבסיס הנתונים נקרא "טור" ומכיל את כל 4 הטורים ביחד. ב-TOC שלו יש:
- "טור סימן ב" (אורח חיים)
- "טור סימן ב" (יורה דעה)
- "טור סימן ב" (חושן משפט)
- "טור סימן ב" (אבן העזר)

## התיקון הסופי ✅

**הפתרון:** במקום להסיר את המילים "חושן משפט" לגמרי, נשתמש בהן כחלק מהסינון ב-TOC!

**הלוגיקה החדשה:**
1. כאשר יש התאמה לראשי תיבות של 3 מילים (למשל "טור חושן משפט")
2. שומרים את המילים האמצעיות (מילה 2 עד מילה N-1) = "חושן משפט"
3. מסירים את כל 3 המילים מהשאילתה → נשאר "סימן ב"
4. מעבירים ל-TOC את **שילוב** של המילים האמצעיות + הטוקנים הנותרים = ["חושן", "משפט", "סימן", "ב"]
5. ה-TOC מסנן רק רשומות שמכילות את **כל** המילים האלה

**הקוד:**
```dart
// שמירת המילים שהתאימו לראשי תיבות (מלבד המילה הראשונה)
final acronymTokensForFiltering = matchedByAcronym && bookQueryTokenCount > 1
    ? queryTokens.skip(1).take(bookQueryTokenCount - 1).toList()
    : <String>[];

// שילוב המילים מראשי התיבות עם הטוקנים הנותרים
final allFilterTokens = [...acronymTokensForFiltering, ...remainingTokens];

final tocEntries = await fetchTocEntries(
  bookId,
  title,
  queryTokens: allFilterTokens.isNotEmpty ? allFilterTokens : null,
);
```

## תוצאה צפויה 🎯

עכשיו כשתחפש **"טור חושן משפט סימן ג"**:
1. המערכת תמצא את הספר "טור" (התאמה לראשי תיבות של "טור חושן משפט")
2. תשמור את המילים האמצעיות: **["חושן", "משפט"]**
3. תסיר 3 טוקנים ("טור", "חושן", "משפט")
4. ישארו הטוקנים: **["סימן", "ג"]**
5. תחפש ב-TOC של הספר "טור" עם הסינון: **["חושן", "משפט", "סימן", "ג"]**
6. תמצא **רק** את "טור חושן משפט סימן ג" ולא את הסימנים ג' מהטורים האחרים! ✅

## דוגמאות נוספות

### דוגמה 1: "שוע אוח יב"
- התאמה: "שוע אוח" (2 מילים) → ספר "שולחן ערוך אורח חיים"
- מילים אמצעיות: ["אוח"]
- טוקנים נותרים: ["יב"]
- סינון ב-TOC: ["אוח", "יב"]
- תוצאה: רק "שולחן ערוך אורח חיים סימן יב" ✅

### דוגמה 2: "משנב ב"
- התאמה: "משנב" (1 מילה) → ספר "משנה ברורה"
- מילים אמצעיות: [] (אין)
- טוקנים נותרים: ["ב"]
- סינון ב-TOC: ["ב"]
- תוצאה: "משנה ברורה סימן ב" ✅

## איך לבדוק?

1. הרץ את האפליקציה
2. פתח 'איתור מקורות'
3. הקלד: **טור חושן משפט סימן ג**
4. בדוק שמופיעה **רק** תוצאה אחת: "טור חושן משפט סימן ג"
5. ודא שלא מופיעים "טור אורח חיים סימן ג" או "טור יורה דעה סימן ג"

## טסטים

נוספו 3 טסטים ב-`test/find_ref_repository_test.dart`:
1. ✅ טסט לראשי תיבות של מילה אחת ("משנב ב")
2. ✅ טסט לראשי תיבות של 2 מילים ("שוע אוח יב")
3. ✅ טסט לראשי תיבות של 3 מילים ("טור חושן משפט סימן ג")

כל הטסטים עוברים בהצלחה!

## סיכום

הבעיה נפתרה! המערכת כעת משתמשת במילים האמצעיות מראשי התיבות כחלק מהסינון ב-TOC, מה שמאפשר לסנן בדיוק את החלק הרלוונטי מספרים מורכבים כמו "טור" או "שולחן ערוך".
