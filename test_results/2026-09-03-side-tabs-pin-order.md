# issue #1134 — כרטיסיות בצד: הנעץ אחרי אייקון ה-PDF

תאריך: 2026-09-03 | ענף: `fix/side-tabs-pin-before-type-icon-1134` | קומיט אימות: `b5a6d193`

## הבאג שאומת

ברצועת הכרטיסיות האנכית (`VerticalReadingTabStrip`) שורת הכרטיסייה בנתה את
אייקון סוג הכרטיסייה (PDF / כלי) ואחריו את הנעץ, ולכן בכרטיסיית PDF מוצמדת
הנעץ נדחק אחרי אייקון ה-PDF. ברצועה העליונה (`CustomTitleBar`) הסדר הפוך:
נעץ, אייקון סוג, כותרת. אומת בקוד ובבדיקה (ב-RTL: pin.dx 759 < pdf.dx 780).

## הפתרון

החלפת סדר שני הבלוקים בשורה — נעץ לפני אייקון הסוג — כמו ברצועה העליונה.

## בדיקות

`test/navigation/reading_tabs_side_panel_test.dart` — בדיקה חדשה: כרטיסיית PDF
מוצמדת בעמודה ← מרכז הנעץ מימין למרכז אייקון ה-PDF. נכשלה על `dev` ועוברת עם
התיקון. הקובץ קיבל `Settings.init` (PdfBookTab דורש אותו).

בדיקות ממוקדות `test/navigation/` + `test/tabs/`: **612 עברו, 0 נכשלו**.

## אימות ויזואלי

לא צולם חי: המשתמש עבד על המחשב בזמן האימות והאוטומציה הופסקה. ההוכחה היא
בדיקת הווידג'ט על העמודה האנכית המלאה (מיקומי הנעץ ואייקון ה-PDF נמדדים
בפיקסלים), והשינוי עצמו הוא החלפת סדר של שני בלוקים בשורה.

## סוויטה מלאה

`flutter test`: **11,632 עברו, 9 נכשלו, 9 דולגו**. הכשלים: הבסיס הסביבתי המוכר
(release_packaging, ‏3×file_sync_compaction, ‏personal_notes_file_backed_book,
‏plugin_spec_freshness, ‏search_scope_menu flake, ‏change_location_dialog)
ו-`hebrew_book_download_test` — flake תחת עומס שעובר בהרצה מבודדת. אפס רגרסיות.
